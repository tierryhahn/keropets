import { AppDataSource } from "../../data-source"
import { Pets } from "../../entities/pets.entity"


const petCreateService = async ({name, species, breed, age, adopted}: IPetsRequest, ongId: string) => {
    
}